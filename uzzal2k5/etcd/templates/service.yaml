apiVersion: v1
kind: Service
metadata:
  name: {{ include "etcd.fullname" . }}
spec:
  clusterIP: None
  selector:
    app: {{ include "etcd.name" . }}
  ports:
    - name: client
      port: 2379
      targetPort: 2379
    - name: peer
      port: 2380
      targetPort: 2380
